export enum TileType {
    Ice = 'Ice',
    Water = 'Water',
    Wall = 'Wall',
    Closed_Door = 'Closed_Door',
    Opened_Door = 'Opened_Door',
}

export enum BoardVisibility {
    Public = 'Public',
    Private = 'Private',
}
