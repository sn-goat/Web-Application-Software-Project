export enum TileType {
    Ice,
    Water,
    Wall,
    Closed_Door,
    Opened_Door,
}

export enum BoardVisibility {
    Public = 'Public',
    Private = 'Private',
}

export enum BoardStatus {
    Ongoing = 'Ongoing',
    Completed = 'Completed',
}
