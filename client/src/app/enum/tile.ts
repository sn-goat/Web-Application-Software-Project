export enum Tiles {
    Default,
    Water,
    Wall,
    OpenDoor,
    CloseDoor,
    Ice,
}

export enum Items {
    Attribute,
    Game,
    Condition,
    Shuffle,
    StartingPoint,
    Flag,
    NoItem,
}
